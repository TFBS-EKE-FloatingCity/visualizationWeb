
<canvas id="HeightChart" height="300" class="chartjs-render-monitor"></canvas>

<script>
    var heightDataConfig = {
        responsive: true,
        type: 'line',
        data: {
            labels: [],
            datasets: [{
                label: 'Height',
                backgroundColor: "rgb(54, 162, 235)",
                borderColor: "rgba(185, 241, 244, 1)",
                data: [],
                fill: true,
            }]
        },
        options: {
            title: {
                display: true,
                text: 'Height (in cm) above sea level' 
            },
            tooltips: {
                mode: 'index',
                intersect: false,
            },
            hover: {
                mode: 'nearest',
                intersect: false
            },
            scales: {
                xAxes: [{
                    display: true,
                    scaleLabel: {
                        display: true,
                        labelString: 'Datetime'
                    },
                    type: 'realtime',
                    distribution: 'series',
                    realtime: {
                        ttl: 60000,
                        duration: 20000,
                        refresh: 1000,
                        delay: 2000
                    },
                    time: {
                        displayFormats: {
                            second: 'HH:mm:ss'
                        }
                    }

                }],
                yAxes: [{
                    display: true,
                    scaleLabel: {
                        display: true,
                        labelString: 'Height'
                    },
                    ticks: {
                        min: 0
                    }
                }]
            },
            //pan: {
            //    enabled: true,
            //    mode: 'x',
            //    rangeMax: {
            //        x: 4000
            //    },
            //    rangeMin: {
            //        x: 0
            //    }
            //},
            zoom: {
                enabled: true,
                mode: 'x',
                rangeMax: {
                    x: 20000
                },
                rangeMin: {
                    x: 10
                }
            }
        }
    };

    $(document).ready(function () {
        // chart
        var ctx = document.getElementById('HeightChart').getContext('2d');
        window.cityHeight = new Chart(ctx, heightDataConfig);

       

        // fill chart with history from DB

        //     $.ajax({
        //         url: '/API/Dashboard/GetSimulationHistory',
        //         dataType: 'json',
        //         type: 'post',
        //         contentType: 'application/json',
        //         processData: false,
        //         success: function (data, textStatus, jQxhr) {
        //             var chartData = []
        //             data = JSON.parse(data)
        //             data.forEach(function (value) {
        //                 setSimData(value.SimTime, value.Wind, value.Sun, value.Consumption)
        //             })
        //             //setSimData(chartData)
        //         },
        //         error: function (jqXhr, textStatus, errorThrown) {
        //             console.log(errorThrown);
        //}
        //     });

        //get Wind, Sun and Consumption Data from Websocket
        setInterval(function () {
            if (typeof wsData !== 'undefined') {
                if (!wsData.SimulationActive) {
                    setCityHeight(Date.now(), heightHY)
                } else {
                    setCityHeight(wsData.Simulationtime, heightHY)
                }

            }
        }, updateRate);
    })

    //Update Chart
    function setCityHeight(currentdate, currentHeight) {
        heightDataConfig.data.datasets[0].data.push({
            x: currentdate,
            y: currentHeight
        })

        window.cityHeight.update()
    }
</script>